# CI repair: 2026-09-16

Failed run: https://github.com/LBBZ/hotShop/actions/runs/34996400919
Base: `904dda06a3412c240f60e91da7008e9bea3efd54`.

## Changes

- Gitleaks: retain the historical red-test evidence and allow only its exact
  deterministic reconciliation issue hash at its exact path under the generic
  API-key rule. This is not a credential. No directory-wide exclusion or history
  rewrite. The introduced-history scan and synthetic credential negative test pass.
- Agent: apply Ruff formatting to `tests/test_registry.py`; no assertions removed.
- PowerShell: scope native error promotion off inside `Invoke-Task19Docker` so
  expected nonzero probes reach `AllowFailure`; unexpected failures still throw.
  A regression with the caller's strict preference enabled verifies both cases
  and that the caller preference remains enabled. Run in both CI workflows.
- RabbitMQ test: use an uncached connection for the consumer channel deliberately
  aborted to prove redelivery. Do not abort a target hidden behind Spring's cached
  channel proxy and then reuse that cache for topology declarations. Keep broker
  failure, redelivery flag, two callback attempts and empty dead queue assertions.

## Local validation

- Gitleaks introduced history `a3e7a6c..904dda0`: 29 commits, no findings after
  the narrowly scoped false-positive exclusion; synthetic credential detection passes.
- Ruff lint and format: pass. Strict mypy src/tests: 54 files pass.
- Agent registry tests: 28 pass.
- Native preference regression, existing native-cleanup regression, CI policy: pass.
- ReliableMessagingContainerTest: 23 pass with isolated real MySQL/RabbitMQ.
  After this run, connection cleanup was tightened with try-with-resources;
  the affected test was rerun on the final source: 1 passed, BUILD SUCCESS.

Remote CI, not this local subset, remains the completion gate. Do not interpret
these results as a full browser, load, or deployment validation.
