# 下一轮优化清单（TASK-21 只记录）

按风险、收益与可验证性排序。以下不是本轮实施承诺，也不改变既有架构。

| 优先级 | 问题/证据 | 建议范围 | 验收方式 |
| --- | --- | --- | --- |
| P1 | Agent工具/确认写入的审计动作、资源不在查询端enum中，`AdminAuditLogQueryService.mapRow`抛错使审计列表500；真实浏览器与SQL已确认。 | 统一所有审计writer与读取契约，兼容已有记录；不删除历史审计。见[阻塞报告](../quality/task-21-blockers.md)。 | 新空库经普通交易、Agent查询/草稿/确认/撤销、库存调整后审计分页与筛选均200；未知历史类型也有明确策略。 |
| P1 | TASK-19 Windows原生真实E2E在Nginx就绪探针前阻断；CRLF here-string进入Alpine sh可复现illegal option，见TASK-21报告。 | 统一验证脚本中跨系统shell载荷的换行处理；审查其他含多行sh的脚本。保持业务断言不变。 | Windows实际checkout和Linux分别运行完整19项真实套件；保留失败前后日志，确认无skip/放宽探针。 |
| P1 | 5000目标未达；TASK-20预约P99约5.09s、异步建单P99约52.09s、47791 dropped，短窗口包含逐次登录。 | 独立区分认证成本和预先持有JWT的纯预约基准；CPU/队列/SQL定位，仍保持唯一用户、一人一单和真实性能门禁。 | 固定机器、预热、持续时长、完成量、dropped、端到端延迟和业务事实对账；容量主张只来自稳态复测。 |
| P2 | 中文售后例句默认deterministic分数0.062578276低于0.15阈值；英文浏览器引用通过；另一次runtime RetrievalError根因尚未定位。 | 分开调查运行时不可用与有限语料召回，增加可诊断的脱敏错误类别和中英评测，不直接降低阈值。 | 同运行镜像/真实Qdrant的中英路由、引用与空结果行为明确；不可用错误有根因证据，无伪造引用。 |
| P2 | 页面刷新后立即全页面导航可中断refresh响应，服务端已轮换而客户端仍持旧Cookie，触发family复用拒绝。 | 评估导航/多标签协调与轮换丢响应恢复策略，不放松重放检测。 | 真实浏览器慢网、导航中断、多标签并发与恶意reuse分开验证；安全保证保留，用户恢复提示明确。 |
| P2 | 对账fence持续变化可长期IN_PROGRESS；seen随本轮唯一预约增长，原始Stream无自动历史裁剪。源码SeckillReconciliationService及分页Lua。 | 研究稳定水位/分段核算和可审计保留策略，不直接删Pending、seen或历史证据。 | 持续写入、多活动、公平推进、崩溃续扫、大历史数据内存预算；真篡改仍告警且合法并发零误报。 |
| P2 | expected库存只以V1.9迁移当时值为基线；同时篡改actual和expected不能检出。 | 明确特权SQL治理、离线核验与独立调整流水对账范围；不引入外键、不假装已有完整事件溯源。 | 单边偏差能检出；双边修改能力边界有明确威胁模型和恢复演练。 |
| P2 | 当前商品库存低于活动总配额时重装可能ACTIVITY_INVALID；活动售出后重装语义有限。见独立复核修复报告。 | 单独设计活动版本、剩余配额和重装API验收；不得用反复seed抹平销量。 | 普通购买/多个活动/已售出/已补偿的重装矩阵，无库存增发、重复预约或错误覆盖。 |
| P2 | 订单SSE每实例最大连接200、2s数据库轮询；Agent运行任务/事件队列保存在进程内。 | 测量SSE连接资源和查询成本；明确Agent进程重启的恢复产品行为，再选择最小改进。 | 多连接断线/重连/Last-Event-ID、认证过期、取消、慢客户端与进程重启；不把网络连接存活当状态正确。 |
| P2 | FakeModel/deterministic的有限用例不能证明真实DeepSeek/Qwen效果；未付费调用。 | 在用户另行授权和预算下运行受控模型对比；Embedding与聊天模型分别评估。 | 固定语料/版本、引用正确率、拒答/权限边界、失败成本和延迟；禁止向工具扩大scope。 |
| P3 | 根Dockerfile COPY整个仓库使文档更新也可能失效构建缓存；首装/镜像拉取成本尚未在另一台新机器测量。 | 单独评估最小构建上下文、缓存层与本机资源建议，保持依赖锁定。 | 空缓存和热缓存各记录拉取/编译/启动时间，校验相同制品和测试范围，不能以本机热缓存代替首装。 |
| P3 | 架构历史报告、旧runbook和默认Compose命令有不同时间范围；旧部署脚本仍属于历史资产。 | 收敛历史入口到带版本声明的归档，增加文档链接/图语法门禁。 | README入口零失效链接、命令无裸生产凭据/危险清理，历史证据不被删除。 |

当前修复、未修与验证结果以[TASK-21验收报告](../quality/task-21-delivery.md)为准。
